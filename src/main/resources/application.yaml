server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stockWise_v1?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

seed:
  enabled: false